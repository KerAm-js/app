import { StyleSheet } from "react-native";

export const intervalInputStyles = StyleSheet.create({
  container: {
    width: '100%',
  },
  inputsContainer: {
    flexDirection: 'row',
    gap: 10,
  },
})