import { StyleSheet } from "react-native";
import { GREY_LIGHT } from "../../../../consts/colors";

export const commentPageStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: GREY_LIGHT,
    paddingTop: 30,
  }
})