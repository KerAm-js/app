import { StyleSheet } from "react-native";

export const callButtonStyles = StyleSheet.create({
  container: {
    width: 50,
    height: 40,
    alignItems: "flex-end",
    justifyContent: "center",
  },
})